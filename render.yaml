services:
  - type: web
    name: core
    buildCommand: |
      pipenv install --deploy --ignore-pipfile
      python manage.py collectstatic --noinput
      python manage.py migrate
    startCommand: gunicorn user_auth.wsgi:application